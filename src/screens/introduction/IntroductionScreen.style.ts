import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {},

  svgContainer: {
    flexBasis: '60%',
    alignItems: 'center',
    justifyContent: 'flex-end',
  },
});
