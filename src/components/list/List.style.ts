import { StyleSheet } from "react-native";
import { size } from "@theme";

export const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    alignItems: "center",
  },
  icon: {
    paddingEnd: size.xs,
  },
});
