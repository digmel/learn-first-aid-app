import { StyleSheet } from "react-native";
import { size } from "@components";

export const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    alignItems: "center",
  },
  icon: {
    paddingEnd: size.xs,
  },
});
