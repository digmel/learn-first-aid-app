import { StyleSheet } from "react-native";
import { size } from "@theme";

export const styles = StyleSheet.create({
  wrapper: {
    marginRight: -size.xl,
  },

  container: {},

  titleContainer: {
    marginBottom: size.m,
    flexDirection: "row",
  },
});
